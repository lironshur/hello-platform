apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: staging-backend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/lironshur/hello-platform
    targetRevision: HEAD
    path: charts/hello-app
    helm:
      valueFiles:
        - values-staging-backend.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
