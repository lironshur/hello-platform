apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-backend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/lironshur/hello-platform
    targetRevision: HEAD
    path: charts/hello-app
    helm:
      valueFiles:
        - values-dev-backend.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
